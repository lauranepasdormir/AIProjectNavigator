# Production Environment Variables
NODE_ENV=production

# Production database URL (this one takes precedence in production mode)
PROD_DATABASE_URL=your_production_db_url_here

# Development database URL (only needed in development)
# DEV_DATABASE_URL=your_development_db_url_here